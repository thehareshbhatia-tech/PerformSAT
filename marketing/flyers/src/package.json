{
  "name": "seva-flyers",
  "private": true,
  "type": "module",
  "description": "Print-flyer build: QR + inlined fonts -> single-page US Letter PDF.",
  "scripts": {
    "fonts": "node inline-fonts.mjs",
    "qr": "node make-qr.mjs",
    "build": "node build.mjs",
    "verify": "node decode.mjs",
    "all": "npm run fonts && npm run qr && npm run build && npm run verify"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "jsqr": "^1.4.0",
    "pngjs": "^7.0.0"
  }
}
