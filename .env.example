# PerformSAT environment variables — example/template.
# Copy to .env.local and fill in your Firebase project credentials.
# (.env.local is gitignored; .env.example is committed.)

# ─── Firebase ──────────────────────────────────────────────────────────────
# From Firebase Console > Project Settings > Your apps > SDK setup and configuration.
REACT_APP_FIREBASE_API_KEY=your-api-key-here
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
REACT_APP_FIREBASE_APP_ID=your-app-id

# ─── App Check (bot/abuse protection) ──────────────────────────────────────
# reCAPTCHA v3 site key registered in Firebase Console > App Check > Apps >
# (web app) > reCAPTCHA v3. When unset, App Check is fully inert. Set it here
# AND in Vercel, deploy, watch App Check metrics until verified-request % is
# stable, and only THEN enforce per service in the console (enforcing early
# locks out users on cached old bundles). See docs/SECURITY_LAUNCH_RUNBOOK.md.
REACT_APP_APPCHECK_SITE_KEY=

# ─── Error monitoring (Sentry) ─────────────────────────────────────────────
# Create a project at sentry.io > Projects > Create (platform: React), then copy
# its DSN here AND into the Vercel production environment. When unset, the app
# falls back to a console-only unhandled-rejection listener (src/index.js) — i.e.
# NO crash visibility in production. Set this before public launch.
REACT_APP_SENTRY_DSN=

# ─── Web Push (re-engagement nudges, Phase 3) ──────────────────────────────
# Generate the key pair at:
#   Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
# Paste the PUBLIC key here. Without it the push opt-in card hides itself and no
# FCM token can be minted (the scheduled sendReEngagementNudges function then has
# nobody to send to). The matching service worker is public/firebase-messaging-sw.js.
REACT_APP_FIREBASE_VAPID_KEY=your-web-push-vapid-public-key

# ─── Feature flags ─────────────────────────────────────────────────────────
# Read via `useFeatureFlag('camelCaseKey')` from src/hooks/useFeatureFlag.js.
# Build-time defaults (set per environment); a localStorage runtime override
# per browser session is also available via `localStorage.setItem('ff:<key>','1')`.
#
# todaysTasks  — Day-4 Today's Tasks hero card on the Dashboard tab.
#                The card replaces the legacy AI Practice Banner when a study
#                plan exists. Set to `true` to gate-on the new dashboard layout.
REACT_APP_FF_TODAYS_TASKS=false
#
# rwDrills     — Day-1-2 R&W focus area drills via the section-tagged
#                weakness contract + R&W bank dispatcher. When `false`, only
#                math weaknesses route to a working drill; R&W weaknesses
#                show a "coming soon" placeholder.
REACT_APP_FF_RW_DRILLS=false
#
# pushNudges   — Phase 3 web-push re-engagement opt-in card on the dashboard.
#                Requires REACT_APP_FIREBASE_VAPID_KEY. The card self-hides when
#                push is unsupported (e.g. iOS Safari outside an installed PWA),
#                already granted/denied, or dismissed.
REACT_APP_FF_PUSH_NUDGES=false
#
# billing      — SEVA Premium (Stripe): 7-day no-card trial, then $50/mo or
#                $250/yr; hard paywall with read-only score access after an
#                unpaid trial. Keep FALSE until the Stripe functions are
#                deployed and prices/secrets are configured (see
#                functions/.env.example). With the flag off the entire billing
#                system is dark: no reads, no function calls, no UI.
REACT_APP_FF_BILLING=false

# ─── Billing function URLs (optional overrides) ────────────────────────────
# Default to the production Cloud Run URLs (same pattern as the AI tutor).
# Only set these for emulator/staging work.
# REACT_APP_ENSURE_ENTITLEMENT_URL=
# REACT_APP_CREATE_CHECKOUT_URL=
# REACT_APP_CREATE_PORTAL_URL=

# ─── Production launch notes ───────────────────────────────────────────────
# rwDrills and todaysTasks now default ON in code (see FLAG_DEFAULTS in
# src/hooks/useFeatureFlag.js) — they are permanent both-sections UX, so they
# no longer need a Vercel env var. Setting REACT_APP_FF_RW_DRILLS=false /
# REACT_APP_FF_TODAYS_TASKS=false here would explicitly DISABLE them (override).
# Still set in Vercel prod: REACT_APP_SENTRY_DSN (above) for crash visibility.
# Analytics events flush to Firestore (services/analyticsService.js) — query
# that collection (or export to BigQuery) to watch signup → activation → WAU.
